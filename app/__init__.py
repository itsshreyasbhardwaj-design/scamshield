"""ScamShield — AI Scam & Fraud Detection (#4), built for users in India.

A deterministic risk engine (messages, links, emails, UPI/crypto handles) behind
swappable provider interfaces (reputation, LLM, database) with mock defaults, so
it runs and tests fully offline with zero keys and flips to live via env vars.
Ships as an installable, mobile-first PWA in English / Hindi / Hinglish."""
__version__ = "1.0.0"
